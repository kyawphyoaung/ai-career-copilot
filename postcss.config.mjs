// postcss.config.mjs

/** @type {import('postcss').AcceptedPlugin[]} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

// Use `export default` for .mjs files (ES Modules)
export default config;

